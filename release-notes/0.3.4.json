{
  "version": "0.3.4",
  "summary": "Fix iCloud sync falsely reporting unavailable when CloudKit is ready.",
  "releaseNotes": {
    "fixed": [
      "Fix Settings showing iCloud temporarily unavailable while signed into iCloud",
      "Fix CloudKit database not opening because ubiquity container URL was required incorrectly",
      "Use CKContainer account status instead of iCloud Drive folder checks for sync availability"
    ]
  }
}
